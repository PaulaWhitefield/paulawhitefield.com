---
title: Blog
layout: "base.njk"
---

<h2>My Blog</h2>

<p>My latest blog articles are here.</p>

{% for article in collections.BlogArticle | reverse %}
  
  <h3><a href="{{ article.url }}">{{ article.data.title }}</a></h3>
  
  <p><i>Authored by {{ article.data.author }} on {{ article.data.date | postDate }} at {{ article.data.time }}.</i></p>
  
  <p>{{ article.data.description }}</p>
  
{% endfor %}